<!DOCTYPE html>
<html>
  <head>
    <%= favicon_link_tag %>
    <title>ShareTheCars - La gestion futuriste de véhicules d'entreprise</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if controller_name == 'pages' && action_name == 'home' || controller_name.start_with?('sessions') || controller_name.start_with?('registrations') %>
      <%= render "shared/navbar" %>
      <%= yield %>
      <%= render "shared/footer" %>
    <% else %>
      <div class="d-flex">
        <%= render "shared/sidebar" %>
        <div class="w-100">
          <%= render "shared/navbar_for_sidebar" %>
          <div class="mx-3">
            <%= yield %>
          </div>
        </div>
      </div>
    <% end %>
    <%= render "shared/flashes" %>
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script>
      particlesJS.load('particles-js', '/particles.json', function() {
      });
    </script>
  </body>
</html>
